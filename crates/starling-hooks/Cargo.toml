[package]
name = "starling-hooks"
version = "0.1.0"
edition = "2021"
description = "SpiderMonkey panic/crash hooks, copied from Gecko"
license = "MPL-2.0"

[dependencies]
arrayvec = "0.7.6"

[features]
panic_hook = []
default_features = ["panic_hook"]
